# 兼容 OpenAI API 的大模型服务(必填)
# 请复制本文件为 .env.local 并填写真实配置(.env.local 不会被提交到 git)
# OPENAI_API_URL 使用完整地址(含 /v1/chat/completions),无需在代码中拼接
OPENAI_API_URL=https://api.openai.com/v1/chat/completions
OPENAI_API_KEY=your-api-key

# 模型名(可选,默认 gpt-3.5-turbo)
# OPENAI_MODEL=gpt-3.5-turbo

# 开发服务器端口(可选,默认 3000)
# PORT=3000

# Gemini(可选;GEMINI_API_URL 为完整 base 地址,如 https://api.xxx.com/google,代码不拼接)
# GEMINI_API_URL=https://generativelanguage.googleapis.com
# GEMINI_API_KEY=your-gemini-api-key
# GEMINI_MODEL=gemini-2.0-flash

# 搜索 API(可选,用于全球检索;需在 .env.local 中填写真实 key)
# TAVILY_API_KEY=your-tavily-api-key
# EXA_API_KEY=your-exa-api-key
